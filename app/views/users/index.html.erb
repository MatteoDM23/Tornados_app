<% provide(:title, 'All users') %>
<body class="index">
    <div class="jumbotron myBackground">
        <h2>All users</h2><br>
        <div class="col-md-6 col-md-offset-3">
        <%= will_paginate %>
            <ul class="users">
                <%= render @users %>
            </ul>
        <%= will_paginate %>
        </div>
    </div>
</body>
<br><br><br>
