<% provide(:title, 'All users') %>
<body class="index">
    <div class="jumbotron myBackground">
        <h2>All users</h2><br>
        <%= will_paginate %>
            <ul class="users">
                <%= render @users %>
            </ul>
        <%= will_paginate %>
    </div>
</body>
<br><br><br>
